meta {
  name: get songs
  type: http
  seq: 2
}

get {
  url: {{base_url}}/songs?sdflkjk=adf
  body: json
  auth: bearer
}

params:query {
  sdflkjk: adf
}

auth:bearer {
  token: {{super_admin_auth_token}}
}
